#
# Cassandra SideCar configuration file
#

cassandra:
  - host: localhost
  - port: 9042

sidecar:
  - host: 0.0.0.0
  - port: 9043
#
# Enable SSL configuration (Disabled by default)
#
#  - ssl:
#      - enabled: true
#      - keystore:
#          - path: "path/to/keystore.p12"
#          - password: password
#      - truststore:
#          - path: "path/to/truststore.p12"
#          - password: password


healthcheck:
  - poll_freq_millis: 30000

cdc:
  - configPath: file:///Users/tgamaethige/Dev/nfcassandra/conf/cassandra.yaml
  - keySpace: testspace
  - columnFamily: movies
  - kafkaServer: kvmkafka_default-deltadev-zone1.cluster.us-east-1.test.cloud.netflix.net:7101
  - kafkaTopic: deltaconnector_cassandra_dev_in
  - outputType: kafka